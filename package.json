{
  "name": "pg-benchmark",
  "version": "1.0.0",
  "description": "Repeatable PostgreSQL query performance benchmark tool. Pulls top queries from pg_stat_statements, runs each N times with EXPLAIN (ANALYZE, BUFFERS), and exports a full Excel report (latency percentiles, buffer/plan analysis, optional concurrency sweep).",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "exceljs": "^4.4.0",
    "dotenv": "^16.4.5"
  }
}
